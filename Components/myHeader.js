import React from "react";
import {
    TouchableOpacity,
    View,
    StyleSheet,
    Text,
} from "react-native";
import db from "../config";

export default class MyHeader extends React.Component{
    render(){
        return(
            <View>
                <Text>
                    1's Say 1's Move
                </Text>
            </View>
        )
    }
}